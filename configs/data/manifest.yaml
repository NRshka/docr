name: manifest
train_manifest: data/manifests/train.jsonl
val_manifest: data/manifests/val.jsonl
test_manifest: data/manifests/test.jsonl
image_root: .
max_text_length: 1024
canvas_length: ${model.canvas.length}
num_workers: 2

